# /etc/rsyslog.conf

# Load necessary modules (imklog is now commented out)
module(load="imuxsock") # for local log sockets
# module(load="imklog")   # for kernel logs - disabled to prevent permission errors in Docker

# Forward all logs to the OTel Collector over TCP
*.* @@otel-collector:5140
